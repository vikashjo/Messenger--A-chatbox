<%= form_with(model: chatroom, local: true) do |form| %>
  <% if chatroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this chatroom from being saved:</h2>

      <ul>
        <% chatroom.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3 text-white">
    <%= form.label "Add a new room" %>
    <%= form.text_field :name, class: 'form-control', placeholder: "Room name" %>
  </div>

  <%= form.submit "Add", class: "btn btn-outline-primary btn-block text-white" %>
<% end %>